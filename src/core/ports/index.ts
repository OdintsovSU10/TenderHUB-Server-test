/**
 * Ports Layer - интерфейсы (контракты)
 *
 * Содержит:
 * - repositories: интерфейсы для доступа к данным
 * - services: интерфейсы для UI-зависимых операций
 */

export * from './repositories';
export * from './services';
